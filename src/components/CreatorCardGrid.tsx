import ContentProvider from "./ContentProvider";

const CreatorCardGrid = () => {
  return (
    <section className="">
      <ContentProvider />
    </section>
  );
};

export default CreatorCardGrid;
